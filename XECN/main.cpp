#include <iostream>

template <typename T>

class no
{
private:
    T content;
    T *prox;

    no(T content)
    {
        this.content = content;
        this.prox = NULL;
    }
public:
    void adicionar_inicio(T no) {
        if(prox = NULL)
            prox = no;
    }
};

int main()
{
    int *head;

    int no nos = new no(NULL, NULL);

    return 0;
}
